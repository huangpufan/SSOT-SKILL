### v2.34

**Upgrade goal**: generalize owner/index/ledger boundaries so STATUS Notes, README/index files, and non-owner SSOT surfaces do not become shadow ledgers for child-entry state, latest validation, run history, or proof-of-work chronology.

**Impact**: `semantic_impact=medium` (new semantic Doctor tags and WARN-only lint heuristics; no new top-level area, STATUS field, lifecycle skill, or high-impact stop-review trigger).

**Review**: Self-review per `status-protocol.md §7.1`.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| STATUS notes ledger | `STATUS.md` area table | Check whether Notes rows maintain child-entry counts, child lifecycle state, latest validation, current green/red state, command pass/fail history, dated batch history, or facts owned by sub-directories / entry files / CI artifacts / bugs / release notes / stop-review evidence. | Notes only contain coverage pointers, uncovered scope, open gap/adjudication pointers, not-applicable rationale, or architecture coverage depth; derived state lives in its owner. |
| Index-derived state | `*/README.md`, Reader Maps, indexes | Check whether indexes copy child body facts, generated counts, recent activity, latest verification, or lifecycle state not explicitly assigned to the index. | Indexes route readers, define area scope, list authoritative owners, and expose only protocol-authorized minimal index fields. |
| Shadow ledger | non-owner SSOT files | Check whether a non-owner file carries chronological task/run/status logs, proof-of-work history, or latest-run rows already owned by STATUS stop-review evidence, bug entries, release notes, commits, CI artifacts, or entry owners. | Chronological evidence is either removed, moved to an authorized owner, or reduced to a stable pattern/policy with owner links. |
| Lint visibility | `ssot-lint.sh` output | Run lint and inspect WARNs for the three new tags; do not treat WARN-only heuristics as semantic proof. | WARNs are reviewed by a human/agent; false positives may be documented, but true positives are routed to owners. |

**Doctor tags**:

- `[STATUS-NOTES-LEDGER]`: `STATUS.md` Notes carry derived child state, latest validation, run history, or proof-of-work chronology instead of coverage pointers.
- `[INDEX-DERIVED-STATE]`: README/index/Reader Map files mirror entry-owned facts, generated counts, latest activity, verification rows, or lifecycle state beyond protocol-authorized minimal fields.
- `[SHADOW-LEDGER]`: a non-owner SSOT surface maintains chronological proof-of-work, task/run/status logs, or latest-run facts already owned elsewhere.

**No-op criteria**:

- Projects whose STATUS Notes already contain only owner pointers, open gaps/adjudications, not-applicable rationale, or coverage-depth notes may record no-op.
- Projects whose indexes only list protocol-authorized fields such as ID/title/status/severity/priority/owner links may record no-op.
- Authorized ledgers such as source-material absorption matrix, core-reference review, stop-review gate, bug entries, decision entries, gotcha entries, tech-debt entries, release notes, and CI artifacts do not need to move.

**Not required**:

- Not required to delete history from the authorized entry file that owns it.
- Not required to make lint WARNs fail CI; `--strict` remains the opt-in hard gate.
- Not required to backfill old task outputs into a new ledger.

### v2.33

**Upgrade goal**: clarify that `testing/` is a stable testing-strategy owner, not a chronological validation ledger. Test command output is evidence for a task, commit, release note, bug entry, or stop-review record unless it changes future test selection.

**Impact**: `semantic_impact=medium` (new semantic Doctor check/tag and cross-skill write-routing rule; no new top-level area, STATUS field, lifecycle skill, or high-impact stop-review trigger).

**Review**: Self-review per `status-protocol.md §7.1`.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Testing ledger boundary | `testing/` | Check whether `testing/README.md` or testing sub-files contain chronological recent-run tables, command transcripts, pass/fail dates, or green/red summaries that do not change strategy, selection, gates, fixtures, current baselines, known gaps, or defensive-test mappings. | Remove or split ledger material out of `testing/`; keep only stable facts with evidence pointers to commits, CI artifacts, bug entries, release notes, final responses, or stop-review records. |
| Stable testing owner set | `testing/` | Check whether testing can answer strategy, selection matrix, gates, fixtures/test data, current baseline, known gaps, and defensive-test source map without forcing readers through run history. | Reader path lands on durable selection guidance and load-bearing guard maps; current baselines are point-in-time expectations, not current green claims. |
| Closeout/audit write discipline | project-specific agent rules or SSOT maintenance docs, if any | Check whether local guidance tells agents to append test results to `testing/` after every task. | Guidance says test results are evidence, and `testing/` changes only when durable testing policy or baseline/gap/map facts change. |

**Doctor tags**:

- `[TESTING-LEDGER]`: `testing/` carries chronological validation logs, recent-run tables, command transcripts, pass/fail dates, or latest-verification rows that do not change stable testing facts.

**No-op criteria**:

- Projects whose `testing/` already contains only strategy, commands, gates, fixtures, baselines, gaps, and defensive maps may record no-op.
- A small project may keep a single current baseline row if it affects future test selection; it should not maintain a full run history.

**Not required**:

- Not required to delete CI artifacts, release notes, bug verification evidence, final-response summaries, or STATUS stop-review evidence.
- Not required to backfill all historical test results into another ledger.

### v2.32

**Upgrade goal**: remove `identity/` as a top-level SSOT area and absorb its remaining useful facts into existing owners. Repository positioning belongs in the opening of `SSOT/README.md`; tech stack, runtime form, repository type, and technical operating stance belong in `architecture/README.md`; primary capabilities stay in `product/prd.md` or the product capability map.

**Impact**: `semantic_impact=medium` (top-level area removal and owner-boundary cleanup; no new top-level area, STATUS field, lifecycle skill, or high-impact stop-review trigger).

**Review**: Self-review per `status-protocol.md §7.1`.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Root positioning owner | `SSOT/README.md` | Check whether the project still needs the one-sentence repository positioning formerly held by `identity/`. | The root README carries the positioning line near the top; it does not create a replacement identity area. |
| Architecture/product absorption | `architecture/`, `product/` | Confirm architecture covers tech stack, runtime shape, repository type, and operating stance; confirm product keeps product promises and primary capabilities. | Identity facts have an existing owner; architecture and product do not duplicate each other. |
| Area and status cleanup | `SSOT/README.md`, `STATUS.md`, startup/thin adapters | Remove the `identity` row, `identity/README.md` routing, and references that treat `identity/` as a current top-level area. | `SSOT/identity/` is deleted or marked obsolete outside the active top-level area set; area counts and links no longer include it. |

**Doctor tags**:

- No new Doctor tag in v2.32. Reuse `[OWNER-ANCHOR]`, `[READER-MAP]`, `[READABILITY]`, or `[CORE-REF]` if identity removal leaves duplicated or stale owner routing.

**No-op criteria**:

- Projects that never had an `identity/` area and already state repository positioning in the root README may record no-op.
- Projects whose architecture/product owners already contain the same facts only need link/status cleanup.

**Not required**:

- Not required to create a replacement top-level area, frontmatter field, or product capability file just to preserve identity wording.
- Not required to mechanically rewrite all docs that use ordinary English words like "identity"; only clean current SSOT area routing.

### v2.31

**Upgrade goal**: add lifecycle metadata to decision entries so ADRs carry
their own temporal and git anchors (`created_on`, `updated_on`,
`introduced_in`, `updated_in`) and bootstrap templates create decision files
with those fields.

**Impact**: `semantic_impact=medium` (decision-entry schema and lint rule; no
new top-level SSOT area, STATUS field, lifecycle skill, or high-impact
stop-review trigger).

**Review**: Self-review per `status-protocol.md §7.1`.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Decision lifecycle fields | `decisions/NNNN-*.md` | Check whether active decision entries carry `created_on`, `updated_on`, `introduced_in`, and `updated_in` when applicable. | Decision entries have stable date and git provenance anchors, or legacy/imported carveout is documented. |
| Decision index readability | `decisions/README.md` | Check whether the decisions index routes by ID/title/status/implementation/date/owner without copying decision body facts. | The index is a route, not a second decision owner. |
| Lint coverage | `ssot-lint.sh` / Doctor output | Run or equivalently check the decision frontmatter lint rule. | Missing required fields are fixed, exempted as bootstrap archaeology, or recorded as gaps. |

**Not required**:

- Not required to fabricate original creation dates for imported historical ADRs.
- Not required to rewrite non-decision Markdown as ADRs.

### v2.30

**Upgrade goal**: extend the cold-reader readability floor SSOT-wide and
replace per-template manual blockquotes with one posture sentence and three
guardrails: prose before tables, define terms positively, and a cell is not a
paragraph.

**Impact**: `semantic_impact=medium` (Doctor readability scope and template
posture change; no new top-level SSOT area, STATUS field, lifecycle skill, or
high-impact stop-review trigger).

**Review**: Self-review per `status-protocol.md §7.1`.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| SSOT-wide readability | user-facing SSOT owner files | Check whether owner bodies start with understandable prose and use tables as indexes/evidence rather than the main explanation. | A cold reader can get the first mental model before tables. |
| Template posture | newly instantiated templates | Check whether templates use the short writing-style pointer and do not recreate the old manual blockquote scaffolding. | New files follow the posture without duplicating protocol prose. |
| Doctor scope | Doctor/readability review | Confirm readability review applies beyond `product/` to all user-facing SSOT body files. | `[READABILITY]` covers the intended owner surfaces. |

**Not required**:

- Not required to rewrite every historical owner immediately.
- Not required to delete tables that function as routes, registers, or evidence indexes.

### v2.29

**Upgrade goal**: introduce a cold-reader readability floor for `product/` so
PRD, product model, roadmap/acceptance, capabilities, and journeys explain the
human/product story before tables.

**Impact**: `semantic_impact=medium` (product readability expectations and
Doctor tag; no new top-level SSOT area, STATUS field, lifecycle skill, or
high-impact stop-review trigger).

**Review**: Self-review per `status-protocol.md §7.1`.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Product prose-first floor | `product/` spine and split owners | Check whether product owners explain what the product is, who uses it, what is promised, and what is not shipped before tables. | Product facts are understandable without reconstructing them from IDs and rows. |
| Capability/journey split readability | `product/capabilities/`, `product/journeys/` | Check whether split entries have a concrete scene and why-separate explanation. | Splits represent stable product boundaries, not one-off implementation/test flows. |
| Architecture boundary | `architecture/` | Check whether architecture links product owners and records technical response/gap instead of rewriting product promises. | Product and architecture do not duplicate each other. |

**Not required**:

- Not required to create capability or journey files for one-off features.
- Not required to copy PRDs verbatim into product files.

### v2.28

**Upgrade goal**: introduce PLTL v3, the Promotable Long-Term Lessons mechanism.
It collapses rank distinctions into apex/authority/inbox and routes user or
repo signals through explicit move blocks.

**Impact**: `semantic_impact=low` (promotion-routing discipline and bundle
capture inbox; no new STATUS field, top-level SSOT area, or Doctor label).

**Review**: Self-review per `status-protocol.md §7.1`; apex moves still use
`$ssot-doctor` stop review.

**No-op criteria**:

- Projects with no pending/promotable long-term lesson signals may record no-op.
- Existing local discipline/gotcha/bug owners do not need rewriting unless a
  specific move is being made.

### v2.27

**Upgrade goal**: reduce bootstrap template ceremony with condition markers for
legacy/optional sections and wide-table splits.

**Impact**: `semantic_impact=low` (template-only defaults; no consumer protocol
rewrite required).

**Review**: Self-review per `status-protocol.md §7.1`.

**No-op criteria**:

- Existing consumer SSOT files continue to function; only new instantiations use
  the thinner optional-block markers and split tables.

### v2.26

**Upgrade goal**: reduce template ceremony by replacing dense STATUS template
blockquotes with one-line pointers and clarifying manifest-vs-STATUS axes.

**Impact**: `semantic_impact=low` (template prose only; no new owner, field, or
stop-review trigger).

**Review**: Self-review per `status-protocol.md §7.1`.

**No-op criteria**:

- Previously instantiated STATUS files do not require mechanical updates; the
  removed prose was redundant protocol restatement.

### v2.25

**Upgrade goal**: first template ceremony-reduction pass: remove repeated
template-instantiation comments, merge adapter templates, and add a template
index.

**Impact**: `semantic_impact=low` (template/installer-source readability; no
consumer SSOT semantic requirement).

**Review**: Self-review per `status-protocol.md §7.1`.

**No-op criteria**:

- Existing consumer templates and SSOT files are unaffected unless they are
  regenerated from the bundle.

### v2.22

**Upgrade goal**: SSOT-SKILL bundle B-class ceremony reduction — reduce STATUS.md, self-review default, protocol upgrade tax by impact, DEC anchor tightening, self-reference lint fix. See CHANGELOG.md for full detail.

**Impact**: `semantic_impact=low` (documentation/editorial changes to protocol reference docs only; no new area, owner field, or stop-review trigger).

**Review**: Self-review per §7.1.

**Subsequent low-impact entries collapsed under this section**:

- **v2.23 — SKILL.md body distillation against `SKILL_STYLE.md` standard**. `ssot-skill/SKILL.md` body 22→11 lines; `ssot-preflight/SKILL.md` body 94→~45 lines; introduces `skills/SKILL_STYLE.md` as contributor-facing acceptance standard for `SKILL.md` prose bodies (five criteria reverse-engineered from `grill-me`). No behavioural change; references, frontmatter routing descriptions, tests, scripts, and templates unchanged. `semantic_impact=low`. Self-review per §7.1. Consumer projects: `tracked_skill_version` may advance to `2.23` as a no-op once it is confirmed that no project-local material quoted the prior SKILL.md wording verbatim.
- **v2.24 — SKILL.md body distillation, second pass (`ssot-bootstrap`, `ssot-closeout`, `ssot-audit`, `ssot-doctor`)**. Each body rewritten against the five `SKILL_STYLE.md` criteria: removed description echoes and lifecycle restatements; replaced step-numbered workflows with second-person posture sentences carrying mode-switch, outcome conditions, and one anti-pattern guardrail each (templates-are-not-evidence; reconcile-the-conversation-not-just-the-diff; segment-before-loading-backlog; run-lint-before-subjective-review). Self-certification bans on waterline advancement / `covered/converged` / bootstrap `passed` / language-lock / `tracked_*` advancement made explicit in each body. No reference, frontmatter routing description, test, script, or template touched. Engineering bucket: `.github/workflows/ci.yml` shellcheck gate tightened to `-S warning` (separate commit, no protocol bump on its own). `semantic_impact=low`. Self-review per §7.1. Consumer projects: `tracked_skill_version` may advance to `2.24` as a no-op once it is confirmed that no project-local material quoted the prior SKILL.md wording verbatim.
