### v2.21

**Upgrade goal**: close the routing blind spot for cross-task agent operation discipline. When a user-confirmed long-term lesson surfaces ("from now on always run real SDK suite before claiming a fix done", "mock-only test does not constitute fixed evidence for external-service behaviour"), the protocol now routes it to `development/` discipline with `Rule / Trigger / Why / Evidence / Failure-mode` fields, instead of collapsing it into a single bug takeaway where future agents cannot recall it. `gotchas/` keeps a `[SHOULD]` trigger field and is reaffirmed as file/module-scoped pitfalls; `development/` discipline is task-pattern-scoped imperative rules. `update-routing.md` gets a new external-SDK file-type row, four new conversation-signal rows for long-term-lesson / mock-only-failure / hotfix-of-hotfix / unrouted-knowledge signals, and a cascade row for discipline discovery. `area-model.md §4.1` adds a fallback classifier for unrouted durable knowledge and explicitly forbids absorbing such knowledge by creating new top-level areas. v2.21 is a semantic protocol extension only; no SSOT top-level area, STATUS field, lint rule, or Doctor label is added or changed.

**Baseline scope**:

- Projects with `tracked_skill_version` less than `2.21`.
- Current `ssot-preflight` `metadata.protocol_version` >= `2.21` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Discipline area exists when warranted | `development/` | If the project has at least one user-confirmed cross-task imperative rule or a recurring procedural failure (mock-only-fixed-then-recurred, hotfix-of-hotfix on SDK contract, etc.), check that a `development/discipline.md` (or equivalent) exists with at least one `DISC-NNNN` entry recording Rule, Trigger, Why, Evidence, Failure mode. | At least the first confirmed discipline is recorded with all fields; `development/README.md` Reader Map links to it; pure projects with no such rule may no-op. |
| Bug takeaway cross-link | `bugs/<entry>.md` | For bugs whose takeaway was generalised into a discipline entry, check that the bug references the `DISC-NNNN` owner instead of duplicating the rule. | Each escalated bug has a one-line cross-link to its discipline owner; no double-source drift between the two. |
| Routing cleanup | adapter material, `SSOT/README.md` task-entry map | Check that any project-local reference to "long-term lesson goes here" routes through `update-routing.md §3` and `area-model.md §4.1` rather than inventing a parallel rule. | No project-local routing rule overrides the new fallback classifier. |
| Reaffirm `gotchas/` trigger field | `gotchas/<entry>.md` | High-risk active gotchas should carry an explicit `Trigger` line per the upgraded `[SHOULD]` marker; cross-task procedural rules previously squeezed into gotchas should be migrated to `development/` discipline. | High-risk active gotchas have triggers or a no-op note; migration of misclassified entries is recorded; small projects with only generic gotchas may no-op. |

**Doctor tags**:

- No new Doctor tag in v2.21. Reuse `[CONSUMPTION]` if a discipline rule is recorded but never linked from `development/README.md` or referenced by the originating `bugs/` entry; reuse `[OWNER-ANCHOR]` if a long-term lesson is duplicated across `development/discipline.md` and a bug takeaway without a clear owner.

**No-op criteria**:

- Projects with no recurring procedural failure and no user-confirmed cross-task rule may record `no-op` and advance `tracked_skill_version` to `2.21` once an independent or self-reviewed pass confirms no impact.
- Existing `gotchas/` entries that are genuinely file/module-scoped need not change beyond the trigger-field clean-up; no mechanical migration of historical gotchas to discipline.

**Not required**:

- Not required to create a `development/discipline.md` for projects with no concrete failure history.
- Not required to back-fill discipline cross-links for `minor` bugs without escalation history.
- No new Doctor label, `ssot-lint.sh` check, STATUS field, or top-level SSOT area.

### v2.20

**Upgrade goal**: expand the installer agent registry to align with the Vercel
skills ecosystem.

**Impact**: `semantic_impact=none` (installer/distribution surface only; no
SSOT protocol semantics, no consumer SSOT content requirement).

**Review**: No protocol stop review required for consumer SSOT content. A
consumer may advance `tracked_skill_version` across v2.20 as a no-op once the
installed bundle version is confirmed.

**No-op criteria**:

- Project SSOT content does not need updates. Only installer usage or supported
  agent keys changed.

### v2.19

**Upgrade goal**: introduce the required top-level `SSOT/01-product/` area as the long-lived product-facts trunk. PRD, product promises, users / operators, product boundary, capability, journey, roadmap intent and product acceptance enter the product owner; architecture only records technical implementation, runtime execution, failure/recovery, observability and implementation gap, and links to the product owner. The current product-routing rule supersedes the legacy v2.8/v2.9 practice of absorbing PRD / product goals directly into architecture views. This release adds product bootstrap templates, Doctor `[PRODUCT]` / `[PRODUCT-OWNER]` / `[PRODUCT-ARCH-DRIFT]` semantic checks, and `ssot-lint.sh` checks for the deterministic product skeleton and the `STATUS.md` product row.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.19`.
- Current `ssot-preflight` `metadata.protocol_version` >= `2.19` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Product skeleton | `SSOT/01-product/`, `STATUS.md` | Create `product/README.md`, `prd.md`, `product-model.md`, `roadmap-and-acceptance.md`, `capabilities/README.md`, `journeys/README.md`; add a `product` row in the area-state table. | Product skeleton exists; the `product` row reflects real coverage and is not marked `covered` because of empty templates. |
| Product source routing | source-material matrix, README/docs/ADR/PRD/product docs | Route PRD, product promises, users / operators, capability, journey, roadmap and product acceptance to the product owner. | Product facts are no longer written into architecture views by default; the source-material matrix points to the product owner. |
| Capability / journey split | `product/capabilities/`, `product/journeys/` | Check that splits only happen for stable product boundaries; one-off feature, ticket, UI script, test case or implementation flow do not get their own product file. | Over-split entries have been merged back into `prd.md` / `product-model.md`; valid splits carry `why separate` and acceptance. |
| Architecture boundary to product | `architecture/README.md`, `architecture/views/*`, domains | Check that architecture links to the product owner and only records the technical response, runtime execution and implementation current/target/gap. | Architecture does not re-define product promises, product non-goals, user journeys or product acceptance; the implementation gap links back to the product owner. |
| Acceptance cascade | `product/roadmap-and-acceptance.md`, `architecture/views/current-target-gap.md`, `testing/` | Check that product acceptance carries the necessary implementation-gap and test-strategy links. | Product acceptance owner is clear; technical gap and verification paths sit in the corresponding technical areas. |
| Deterministic product skeleton | `ssot-lint.sh`, `SSOT/01-product/`, `STATUS.md` | Run deterministic lint or equivalent checks; only confirm the required product skeleton and `STATUS.md` product-row shape. | Structural gaps have been filled; lint results do not replace product / architecture semantic review. |
| Product boundary semantic review | Doctor output, audit/stop review | Run `[PRODUCT]`, `[PRODUCT-OWNER]`, `[PRODUCT-ARCH-DRIFT]` semantically. | stale items fixed, adjudicated or recorded no-op; only after pass may `tracked_skill_version` advance. |

**Doctor tags**:

- `[PRODUCT]`: required product skeleton missing, or PRD, product promises, capability, journey, roadmap or product acceptance not entered into the product owner.
- `[PRODUCT-OWNER]`: product Reader Map / owner index duplicates body facts, capability/journey is over-split, or a product fact has no single highest stable owner.
- `[PRODUCT-ARCH-DRIFT]`: architecture rewrites, duplicates or alters product-owner facts; should be changed to a link to the product owner and only record the technical response or implementation gap.

**No-op criteria**:

- A library / tool with no external PRD still requires the product skeleton; users, promise, non-goals and acceptance can be written as minimal product facts, with unknown parts marked `gap` / `unknown`.
- The technical operating model already in architecture views may stay; only migrate product facts to the product owner and rewrite the architecture parts to link to the owner.
- Mechanical creation of capability/journey entry files is not required; create them only when a stable product boundary meets the split criteria.

**Not required**:

- Not required to wholesale move `architecture/views/critical-journeys.md` into product.
- Not required to copy the full external PRD verbatim.
- Not required to add commands, skills, JSON/YAML schemas or issue-tracker workflows.

### v2.18

**Upgrade goal**: Architecture evidence-guided decomposition and bottom-up synthesis. Strengthen four native architecture capabilities: use code relations and runtime evidence as decomposition clues; converge bottom-up via `domain evidence -> view synthesis -> root Reader Map`; establish a unique authoritative owner anchor for each key long-lived claim; upgrade Doctor architecture L2 checks to rubric-style stale output. This release only changes Markdown protocol semantics and Doctor tags; no new skills, SSOT top-level areas, JSON/YAML state files, schemas, commands or auto-indexers.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.18`.
- Current `ssot-preflight` `metadata.protocol_version` >= `2.18` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Evidence-guided decomposition | `architecture/README.md`, `architecture/domains/README.md`, domain README | Check whether `decomposition_basis` records entrypoints, call/dependency edges, shared state/resource, runtime flow, failure/recovery boundary, contract surface, tests, configs, scripts, ADR/source material and other candidate signals, and states rejected axes / false friends. | Decomposition is supported by state, failure, contract, lifecycle, verification or current/target/gap semantics; not mechanically mirrored to source directories, package names, class names, teams or external topic trees. |
| Candidate evidence boundary | architecture root/views/domains, source-material matrix | Check whether dependency graph, IDE diagrams, externally generated diagrams, auto-summaries and source material serve only as candidate clues; whether current facts return to code, config, schema, test or runtime evidence. | Candidate diagrams/summaries do not directly support `covered`; unverified content is written as `gap` / `unknown` / pending. |
| Bottom-up synthesis | architecture domains, views, root README | Check whether major bootstrap, architecture audit or reorganization converges via domain evidence -> view synthesis -> root Reader Map; whether root/view claims have domain evidence or explicit gap. | Root does not write sweeping conclusions first; views synthesize multiple domains but do not take over state/contract/failure detail; unverified conclusions are demoted. |
| Reader Map as reader route | `SSOT/README.md`, `architecture/README.md`, `architecture/views/README.md`, domain README | Check whether the Reader Map records reader intent, first stop, authoritative owner, evidence direction, stop condition, risk, rather than maintaining fact text. | Reader Map only routes to the owner; carries no independent long-lived facts. |
| Authoritative owner anchor | architecture views/domains, engineering areas, decisions, README/docs/ADR/PRD | Check whether each key long-lived claim has a unique owner: domain, view, engineering area, decision or other existing SSOT authoritative area. | Non-owner locations only link to the owner; duplicate fact sources are deleted, thin-linked or marked as conflict. |
| Architecture rubric | Doctor output, `STATUS.md` stop-review gate | Does Doctor output stale items per the root/view/domain rubric: 1-minute mental model, view rationale, domain why separate, owner/evidence/why/risk/constraint, required diagrams, current/target/gap separation. | stale items fixed, adjudicated or recorded no-op; only after pass may `tracked_skill_version` advance. |

**Doctor tags**:

- `[DECOMPOSITION]`: decomposition has no evidence-guided signals, only mirrors directory/package/class/team or external topic tree, lacks rejected axes / false friends, or a domain lacks an effective `why separate`.
- `[SYNTHESIS]`: root/view writes a sweeping conclusion without domain evidence, view takes over domain detail, or unverified content not marked `gap` / `unknown`.
- `[OWNER-ANCHOR]`: the same long-lived fact maintained in multiple places, key claim lacks a unique authoritative owner, or Reader Map / root summary holds independent facts.
- Continue reusing `[READER-MAP]`, `[READABILITY]`, `[DIAGRAM]`, `[ARCH-ROOT]`, `[ARCH-VIEWS]`, `[ARCH-DOMAINS]`.

**No-op criteria**:

- A small single-level repo may no-op as long as stop reason, owner anchor, evidence summary and reviewer challenge are complete; no forced domain split required.
- When evidence is insufficient, `gap` / `unknown` is allowed; no need to fabricate dependency graph, Mermaid diagram detail or current facts.
- An existing Reader Map that only does reader-route, links to owners and holds no independent facts may no-op.
- An existing root/view/domain with key long-lived claims that already have unique owners and non-owner positions only linking, may no-op.

**Not required**:

- No new SSOT top-level area, STATUS field, JSON/YAML state file, schema, command or skill.
- Not required to mechanically migrate old projects to the latest `views/ + domains/` template; only fix scope that affects authority, evidence and reader-route.
- Not required to integrate Tree-sitter, LSP, automatic dependency graph, code indexer or generator.
- Not required to modify `ssot-lint.sh`; these are L2 semantic checks.

### v2.17

**Upgrade goal**: split the monolithic `ssot-skill` into a process-triggered SSOT Skill bundle. The formal entry becomes `ssot-preflight`, responsible for pre-task STATUS perception, minimal read routing, protocol waterline comparison and in-process write trigger list; `ssot-bootstrap`, `ssot-closeout`, `ssot-audit`, `ssot-doctor` carry initialization, change-batch closeout, proactive catch-up and health/stop review respectively; `ssot-skill` remains only as a lightweight compatibility shim. The unique authoritative version owner migrates to `ssot-preflight/SKILL.md`.

**Baseline scope**:

- Projects with `tracked_skill_version` less than `2.17`.
- Current `ssot-preflight` `metadata.protocol_version` >= `2.17` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Entry skill rename | Startup adapters, `SSOT/README.md` task entries, runtime prompts | Update "use `ssot-skill` at task start" to `$ssot-preflight`; old `$ssot-skill` is only a compatibility shim. | New task entries can clearly trigger `$ssot-preflight`; no new document treats the shim as the protocol owner. |
| Lifecycle split routing | Startup adapters, mission prompt, bootstrap prompt, development-process docs | Add process trigger points for `$ssot-bootstrap`, `$ssot-closeout`, `$ssot-audit`, `$ssot-doctor`. | Each skill has a clear trigger moment; no skill added by content domain without a trigger point. |
| Protocol owner migration | `STATUS.md` protocol waterline notes, Doctor/Audit text | Change protocol version source to `ssot-preflight/SKILL.md`. | `tracked_skill_version` semantics points at the bundle protocol version, not the legacy `ssot-skill/SKILL.md`. |
| Open adjudication parser | runtime hooks / preflight text | Standardize the canonical heading as `## open adjudications`, compatible with the old `## pending adjudications`. | pending or expired-deferred entries can be correctly recognized by preflight/runtime. |
| Install surface | Project-level skill install | Install all sibling skills and keep the lightweight `ssot-skill` shim. | Under `.codex/skills/` there are 5 formal skills + shim; source aligned with the install surface. |
| Adapter / core-ref boundary | Doctor / lint / startup reference file review | Explicitly define thin-adapter as the optional entry form generated or hosted by SSOT; handwritten / mixed startup files are not reported as `[ADAPTER]` for lacking a generated marker. | `[ADAPTER]` only covers generated thin-adapter form; missing SSOT routing goes to `[CONSUMPTION]`; commands, workflows, architecture and test facts go to `[CORE-REF]`. |

**Doctor tags**:

- `[SKILL-SPLIT]`: multi-skill bundle missing formal entry, process skill, shim, version owner or process-trigger description.
- `[SKILL-PROTOCOL]` continues to be used when `tracked_skill_version` is missing or behind.

**Not required**:

- No new SSOT top-level area, JSON/YAML state file or product-level Web/API truth.
- Not required to install each process skill separately; in the first phase of v2.17, install together as a bundle.
- Not required to mechanically rename all historical text; only modify the authoritative locations that affect triggering, protocol waterline and runtime hints.

### v2.16

**Upgrade goal**: remove all named entries, source-material classifications and special routing of the old externally-generated repository-material surface. SSOT retains readability capabilities, but these are now native protocol: Reader Map is generated from architecture decomposition, reader questions and evidence; claim-to-evidence is a writing standard; script/tool directories come from repo scripts, manifests, CI and config; externally generated diagrams, screenshots, dependency graphs and auto-summaries are only ordinary diagram candidates or user-provided external material candidates, and cannot become authoritative structure.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.16`.
- Current `ssot-preflight` `metadata.protocol_version` >= `2.16` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Legacy generated surface cleanup | Entire `SSOT/` tree, adapters, STATUS source-material matrix | Search for legacy parallel generated directories, old-name entries, special matrix rows sourced from external automatic topic trees, or routing rules; remove parallel authority surfaces and re-route still-valuable facts as ordinary source material that is re-verified. | The project SSOT no longer exposes old names or dedicated directories; no treating of external topic trees as authority. |
| Source material scope | `STATUS.md`, project-local copy or local convention of `source-material.md` | Source-material scope only retains README/docs/ADR/PRD/runbook/design docs/subsystem README/user-provided external material; external auto-summaries can only be ordinary external-material candidates. | No source-material classification, state or input description dedicated to accepting legacy automatic knowledge surfaces. |
| Reader Map provenance | `SSOT/README.md`, `architecture/README.md`, views/domain README | Check whether the Reader Map is generated from decomposition_basis, reader questions, authoritative locations and evidence, not borrowed from external topic trees. | Reader Map only routes, carries no independent facts; topics come from this repo's architectural decomposition and evidence. |
| Evidence writing standard | architecture views/domains, development/testing/release/deployment | Check whether key claims are maintained per native claim-to-evidence writing with owner, evidence, why/risk/constraint. | Key claims have evidence and authoritative owner; unverified content is written as gap/unknown/not_applicable. |
| Script/tool inventory provenance | `development/`, `testing/`, `release/`, deployment-related areas | Check whether script/tool inventories come from repo scripts, package manifest, CI, Makefile, config or actual run validation. | Script inventory is not unverified copy from external auto-summary; purpose, preconditions and risk have evidence. |
| Generic diagram candidate governance | architecture root/views/domains | Check whether externally generated diagrams, screenshots, IDE dependency graphs or auto dependency graphs are only candidates, and are rewritten as maintainable Mermaid before becoming authoritative diagrams. | External diagrams do not directly support covered; authoritative diagrams have Diagram ID, Status, Covers, evidence and owner. |

**Doctor tags**:

- `[LEGACY-SURFACE]`: legacy parallel generated knowledge surfaces, old-name entries, dedicated source-material classifications still exist, or external topic trees/auto-summaries are used as authoritative structure.
- Reuse `[READER-MAP]`, `[READABILITY]`, `[DIAGRAM]`: check native Reader Map, readable authoritative body and generic diagram-candidate governance respectively.

**Not required**:

- No migration script; the protocol upgrade audit is enough.
- Not required to mechanically rewrite all existing SSOT files to the latest template.
- Not required to proactively fetch any externally generated material.
- No new SSOT area, STATUS field, schema or generator.

### v2.15

**Upgrade goal**: fill in the "core reference document" audit slice and establish the SSOT readability protocol. `AGENTS.md`, `CLAUDE.md`, `.cursor/rules`, `.windsurf/rules`, `GEMINI.md` and other startup-read / Agent-rules files cannot be checked only as thin-adapter structure; if they carry repo constraints, commands, directory maps, workflows, architectural boundaries, model/config rules or test strategy, they must be audited continuously as source material and core reference documents. Add the core-reference-document review table in `STATUS.md`; add Doctor `[CORE-REF]` L1/L2 checks. Add the Reader Map / quick-understanding map for entry routing; add the Readable Authority / readable authoritative body, requiring the body to use short narrative, claim, evidence, why, risk and constraint to build a mental model; claim-to-evidence becomes the writing standard for key claims; script/tool directories enter engineering operation areas; externally generated diagrams, screenshots and dependency graphs are only diagram candidates. `[ADAPTER]` continues to cover only thin-adapter file form, `[CONSUMPTION]` continues to cover SSOT trigger chain. No new SSOT top-level area, no change to the semantic boundary of `ssot-lint.sh`.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.15`.
- Current `ssot-preflight` `metadata.protocol_version` >= `2.15` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Core-reference-document inventory | `STATUS.md` | Find `AGENTS.md`, `CLAUDE.md`, `.cursor/rules/*`, `.windsurf/rules/*`, `GEMINI.md` or equivalent startup reference files; record each in the core-reference-document review table. | Present means entered; if no file and project has no harness requirement, record `not_applicable`. |
| Source-material classification | source-material absorption matrix / corresponding authoritative location | Classify startup reference files containing long-lived facts per [`source-material.md`](../../../ssot-preflight/references/source-material.md); long-lived facts are absorbed into a unique authoritative location like architecture/development/testing/decisions. | When long-lived facts exist, complete absorb / link / conflict records; pure thin adapters may no-op. |
| Fact correctness audit | The core reference documents themselves | Compare commands, directory maps, workflow state, architectural constraints, model/config rules, test strategy in the file against code, manifests, Makefile/CI, SSOT and the current protocol. | For each item give `update-doc` / `thin-adapterize` / `absorb-to-SSOT` / `record-conflict` / `no-op`; do not silently absorb drift. |
| `[ADAPTER]` / `[CONSUMPTION]` / `[CORE-REF]` separation of duties | Doctor / adapter strategy | Confirm `[ADAPTER]` only covers marker, line count, optional source hash and summary boundary; `[CONSUMPTION]` covers SSOT trigger chain; `[CORE-REF]` covers fact correctness. | Doctor output can distinguish file form, trigger chain and fact issues. |
| Script boundary | `ssot-lint.sh` usage | Do not stuff command/workflow/architecture-fact semantic judgement into deterministic lint. | Existing scripts may continue to run; if no script change, record no-op. |
| Reader Map / quick-understanding map | `SSOT/README.md`, `architecture/README.md`, `architecture/views/README.md`, domain README | Check whether the entry only links to authoritative locations and carries no independent long-lived facts; whether root can show reader questions, authoritative locations, key evidence and risks at a glance. | The entry can quickly route the reader; fix or mark gap when stale, broken or written as a second fact source. |
| Readable Authority / readable authoritative body | architecture views/domains and applicable engineering operation areas | Check whether the body has only tables/lists; whether key claims have evidence, owner, why/risk/constraint; whether `gap` / `unknown` / `not_applicable` is written when evidence is insufficient. | Body has short narrative first, then table evidence; do not fabricate to fill the template. |
| Claim-to-evidence writing | architecture views/domains and applicable engineering operation areas | Check whether key claims have stable evidence links, owner, why/risk/constraint; whether `gap` / `unknown` / `not_applicable` is written when evidence is insufficient. | Key claims are traceable; do not only write conclusions or vague summaries. |
| Diagram candidate governance | architecture root/views/domains | Check whether externally generated diagrams, screenshots or dependency graphs are only diagram candidates; whether authoritative diagrams are Mermaid fenced blocks with metadata, evidence, state and current/target separation. | When required diagrams are missing, external diagrams are used directly as authoritative, evidence is missing or current/target are mixed, the related scope cannot be marked `covered`. |
| Engineering script inventory | `development/`, `testing/`, `release/`, deployment-related areas | If source material or code shows script/tool inventories, check whether absorbed into engineering areas and link back to the architecture owner if needed. | development/testing/release use the new inventory fields; script inventory is not piled at architecture root. |
| Doctor semantic checks | Doctor output, `STATUS.md` | Add or equivalently run `[READER-MAP]`, `[READABILITY]`, extended `[DIAGRAM]` checks. | stale items fixed, adjudicated or recorded no-op; only after pass may `tracked_skill_version` advance. |

**Doctor tags**:

- `[CORE-REF]`: core reference documents missing the STATUS inventory, or their commands, directory map, workflow state, architectural constraints, model/config rules, test strategy inconsistent with code/SSOT/protocol.
- `[READER-MAP]`: entry map missing, stale, points at non-authoritative location, or carries independent long-lived facts in the entry.
- `[READABILITY]`: body has only tables/lists, lacks one-sentence conclusion, why/risk/constraint, owner or evidence.
- `[DIAGRAM]`: continues to cover required Mermaid diagrams missing, insufficient state/evidence, mixed current/target; new scenarios include externally generated diagrams, screenshots or dependency graphs used directly as authoritative diagrams.

**No-op criteria**:

- Project has no core reference documents and no project constraint that a harness needs a startup file; recording `not_applicable` in `STATUS.md` is enough.
- Core reference document is already a thin adapter, `[ADAPTER]` file form compliant, `[CONSUMPTION]` static chain compliant, and does not contain independent long-lived facts; the core-reference-document review table records `covered / thin-adapter / no-op`.
- The file contains long-lived facts but this audit has confirmed they are consistent with code, SSOT and protocol, and authority relationships and recommended actions are clearly recorded; may record `covered`.

**Not required**:

- No new `SSOT/core-reference/` or other top-level area.
- Not required to mechanically replace user-written `AGENTS.md` / `CLAUDE.md`; default to producing concrete change suggestions; only change the startup file itself when the user authorizes or the task clearly requires.
- Not required to automate `[CORE-REF]` semantic check into `ssot-lint.sh`.
- Not required to create a startup file for a project without the corresponding harness.
- Not required to fabricate complex Reader Map or diagrams for small repos; when evidence is insufficient, write `gap` / `unknown` / `not_applicable`.
- No new top-level parallel knowledge area, new schema, new STATUS field or generator.
- Not required to proactively fetch externally generated material; only handle user-provided material as ordinary source material.

### v2.14

**Upgrade goal**: fill in the "trigger / consumption side" behavioural feedback loop -- fulfilling the L4 behavioural probe that the protocol has promised since v2.13 but never defined (the dangling reference in line 294 comment of [`ssot-lint.sh`](../../../ssot-doctor/assets/scripts/ssot-lint.sh) and the CONSUMPTION check item in [`doctor.md`](../../../ssot-doctor/references/doctor.md)). Add [`consumption-audit.md`](../../../ssot-doctor/references/consumption-audit.md) as the semantic owner of trigger-side verification: probe whether SSOT is triggered and used from real conversation transcripts, locate the failed link, produce trigger improvement suggestions. All optimization actions only suggest by default; change only after user authorization. No new SSOT area, no new STATUS field, no introduction of generators or script-driven LLM calls.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.14`.
- Current `ssot-preflight` `metadata.protocol_version` is `2.14` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Near-field trigger probe | Session self-check flow | Behaviour driven by skill protocol; project SSOT files themselves need no modification. | Confirm no-op; subsequent Session self-checks incidentally do near-field probes. |
| Far-field consumption audit | Project transcripts (on demand) | When user names it or near-field signal is insufficient, scan historical transcripts per [`consumption-audit.md`](../../../ssot-doctor/references/consumption-audit.md) and evaluate trigger health. | `[MAY]` enable; not triggered records no-op. |
| Trigger-side optimization suggestion | Project adapter / `SSOT/README.md` navigation / skill body | By default only produce suggestions; only change the corresponding files after user authorization. Changing the skill body needs independent stop review + version bump as appropriate. | Without authorization only record suggestions and no-op; with authorization change at the attribution layer and record stop review. |
| L4 reference loop closure | `doctor.md` / `ssot-lint.sh` comments | Behaviour driven by skill protocol; project SSOT needs no modification. | Confirm no-op. |

**Doctor tags**:

- Reuse existing `[CONSUMPTION]`: L1 static-chain break is still marked by it; L4 behavioural attribution and trigger-side optimization go to consumption audit; no new tag.

**Not required**:

- Not required to run far-field transcript analysis every session (near-field probe is zero-cost resident; far-field is on demand).
- Not required to auto-rewrite any trigger-side file; default to only suggestions.
- No new SSOT area, STATUS field or must-create file.
- Not required to force far-field probing for harnesses without obtainable transcripts; degrade and record when location fails.

### v2.13

**Upgrade goal**: based on external project memory-system research, do engineering hardening -- collapse the mechanically judgeable checks in the protocol into `ssot-lint.sh`, and fill in four anti-corrosion capabilities: (1) write-time conflict / negation scan; (2) evidence pointer symbol anchor (`path#symbol`) resistant to code movement; (3) thin-adapter source hash drift detection; (4) SSOT consumption chain validation. All changes add no SSOT area, no STATUS field, no generator or index layer.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.13`.
- Current `ssot-preflight` `metadata.protocol_version` is `2.13` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Write-time conflict / negation scan | SKILL.md §3.1 write preconditions | Behaviour driven by skill protocol; project SSOT files themselves need no modification. | Confirm no-op; subsequent writes follow the new preconditions. |
| Evidence pointer symbol anchor | confidence frontmatter with `evidence: path:line` | Old line-number pointers are not required to be migrated mechanically; change to `path#symbol` when the line number becomes invalid. See [`knowledge-integrity.md`](../../../ssot-preflight/references/knowledge-integrity.md) §5.1. | No invalid pointers means no-op; if invalid, change to symbol anchor in place. |
| gotcha executable contract | `gotchas/` | Check whether active gotchas come with "don't do / do this instead" pairs. | Fill in executable avoidance for high-risk entries; generic entries may skip. |
| Adapter source hash line | AGENTS.md / CLAUDE.md and other adapters | If the project uses thin adapters, may add `SSOT-source: <path>@<hash>` lines in the marker to enable drift detection. See [`adapter-strategy.md`](../../../ssot-doctor/references/adapter-strategy.md) §2-§3. | `[MAY]` enable; if not enabled, record no-op, not mandatory. |
| Consumption chain | Adapter + `SSOT/README.md` | Check whether the adapter contains an SSOT read instruction and routes to SSOT, and whether the README navigation entry exists. | Fill the missing routing / entry; otherwise no-op. |
| ssot-lint checks 6-9 | Project CI / Doctor flow | The extended [`assets/scripts/ssot-lint.sh`](../../../ssot-doctor/assets/scripts/ssot-lint.sh) may automate the new L1 checks. | `[MAY]` introduce as CI / Doctor pre-gate; the script is called from the skill path, no copy needed. |

**Doctor tags**:

- `[CONSUMPTION]`: SSOT consumption chain broken -- adapter does not route to SSOT, SSOT path unreachable, or README navigation entry missing.

**Not required**:

- Not required to mechanically migrate existing `path:line` evidence pointers to symbol anchors.
- Not required to fill executable contracts for all gotchas (high-risk first).
- Not required to enable source hash lines for thin adapters (`[MAY]`).
- No new SSOT area, STATUS field or must-create file.

### v2.12

**Upgrade goal**: based on the origin project's pilot instantiation feedback, do three governance improvements -- (1) protocol-rule layering MUST/SHOULD/MAY to lower the execution threshold under high-load tasks; (2) the independent stop-review gate adds a `self-reviewed` demotion path, allowing medium-impact scope to be self-reviewed by the updater with explicit recording; (3) the STATUS.md coverage state table is forbidden to maintain derivable information (entry counts etc.), eliminating an entire class of sync drift.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.12`.
- Current `ssot-preflight` `metadata.protocol_version` is `2.12` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Protocol-level convention | SKILL.md reading | The agent recognizes `[MUST]`/`[SHOULD]`/`[MAY]` markers when reading SKILL.md; project SSOT files do not need annotation. | Behaviour change is driven by skill protocol, no project-SSOT modification needed; confirm no-op. |
| self-reviewed path | STATUS.md stop-review gate | Check whether existing stop-review tables have entries that should have been marked `self-reviewed` but were missed. | Historical entries keep the actual recorded `reviewer` field; new entries use §1.3 distinction. |
| De-redundancy of notes column | STATUS.md coverage state table | Check whether the notes column of the `area / state / notes` table maintains derivable information (entry counts, sub-directory entry state, recent test results). | When redundant, change to a pure pointer to the sub-directory README; notes only keep unique semantics. See [`status-protocol.md`](../../../ssot-preflight/references/status-protocol.md) §3.0. |
| Minimal compensation for legacy-mode design intent | `architecture/README.md` following the legacy direct child-domain route | Check whether the root README contains the three sections: design brief, core invariants, Current / Target / Gap summary. | When any section is missing, use the `[MUST]` marker to fill at least the minimal narrative; no forced creation of the `architecture/views/` directory. See [`architecture.md`](../../../ssot-preflight/references/architecture.md) §2.1. |
| Bootstrap recon archive | Bootstrap Phase 4 cleanup flow | Check whether there are cleanup actions a future bootstrap will perform; for already-bootstrapped projects whose recon.md is deleted, no remediation is needed. | New bootstrap follows the improved archive path (recon.md -> `decisions/0000-bootstrap-recon.md`). Completed projects record no-op. See [`bootstrap.md`](../../../ssot-bootstrap/references/bootstrap.md) §5. |
| ssot-lint script availability | Project CI / Doctor flow | Check whether the project can use `assets/scripts/ssot-lint.sh` to automate L1 checks. | `[MAY]` introduce as CI or Doctor pre-gate; the script itself need not be copied to the project, can be called directly from the skill path. See [`doctor.md`](../../../ssot-doctor/references/doctor.md) §2.1. |

**Not required**:

- Not required to traverse all historical stop-review entries to add `self-reviewed`.
- Not required to add any SSOT files or directories.
- No introduction of new must-create files or areas.

### v2.11

**Upgrade goal**: introduce the knowledge integrity protocol (confidence state machine), layered verification protocol (Doctor L1/L2) and thin-adapter protocol (AGENTS.md/CLAUDE.md generation spec), forming a closed-loop governance of knowledge creation -> verification -> ecosystem adaptation.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.11`.
- Current `ssot-preflight` `metadata.protocol_version` is `2.11` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Knowledge integrity protocol | All SSOT files containing `confidence: inferred` or `needs_verification: true` | Check whether old-format confidence tags exist. Legacy `inferred` is an equivalent expression of `candidate`; no mechanical migration required. | Agent handles old format by equivalence; if no old format, record no-op. |
| confidence write-location compliance | architecture views/domains authoritative body | Check whether `confidence: hypothesis` content exists in architecture views/domains. | Migrate hypothesis content to gotchas (tag source) or STATUS.md open gaps; if no such content, record no-op. |
| confidence blocks covered | STATUS.md coverage state table | Check whether areas marked `covered` contain `confidence: hypothesis` or `candidate`. | If present, demote coverage to `gap`; if absent, record no-op. |
| Doctor L1/L2 layering | Doctor check flow | Confirm Doctor runs in L1 -> L2 order and includes `CONFIDENCE` and `ADAPTER` check items. | Reading behaviour driven by skill protocol; project SSOT files need no modification; confirm no-op. |
| Thin-adapter audit | AGENTS.md, CLAUDE.md, .cursor/rules etc. | Check whether the project has agent harness instruction files. If present and they contain independent long-lived facts, suggest migrating to SSOT and replacing with thin adapters. | When such files exist, record a migration suggestion or no-op (if the file is already a thin adapter or does not exist). Not required to force-replace user-written agent instruction files. |

**Not required**:

- Not required to mechanically traverse all SSOT files and replace old confidence formats.
- Not required to force-replace existing user AGENTS.md/CLAUDE.md with thin adapters.
- Not required to create adapters for projects without agent instruction files.
- No new must-create files or areas.
- Not required to mechanically rewrite existing SSOT files.

### v2.10

**Upgrade goal**: enrich memory coverage for agent use cases across the full development lifecycle -- so the agent can not only understand architecture and process, but also correctly write style-consistent code, proactively check matching pitfalls before operation, understand defensive testing rationale, and proactively discover external-integration quirks during bootstrap. This change does not add areas or introduce hard blockers; it only refines content requirements and routing rules of existing areas.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.10`.
- Current `ssot-preflight` `metadata.protocol_version` is `2.10` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| development/ coding conventions | `development/` | Check whether the project has coding conventions, end-to-end skeleton flows or agent operational prerequisites beyond linter coverage. | When such knowledge exists, record pattern language, skeleton flow and preconditions; otherwise record no-op. Not required to fabricate non-existent conventions. |
| gotchas/ trigger condition | `gotchas/` | Check existing `active` gotcha entries to see whether `trigger condition` can be added for precise routing. | Add trigger conditions for high-risk gotchas; generic pitfalls may skip. Recommend including trigger condition for new gotchas. |
| testing/ defensive-test origin | `testing/` | Check whether `bugs/` contains `critical` / `major` / `recurred` entries with identifiable defensive tests. | When such bugs exist, add a defensive-test origin subsection in testing/; otherwise record no-op. |
| Bootstrap external-dependency exploration | Bootstrap evidence-source map (only affects new bootstrap) | Confirm the bootstrap evidence-source map includes the external-dependency/integration row. | Already included; no modification needed for existing project SSOT. |
| Read-routing update | `SSOT/README.md` (task-entry map, if exists) | Confirm whether the agent's read flow has been routed per new rules: when adding a feature, read the development/ pattern language; drill down proactively when a gotcha trigger condition matches. | Reading behaviour driven by skill protocol; project SSOT files need no modification; confirm no-op. |

**Not required**:

- Not required to mandatorily add trigger conditions to all gotchas (the field is recommended, not required).
- Not required to fabricate development/ pattern-language content for projects without coding conventions.
- Not required to retrospectively establish testing/ defensive-test origins for all historical bugs.
- No new Doctor hard blocker; new fields in this release are all optional/recommended.
- Not required to mechanically rewrite existing SSOT files.

### v2.9

**Upgrade goal**: upgrade architecture views from optional absorption slots to SSOT's design-intent layer, preventing architecture from degenerating into a tabulated audit directory. The new protocol requires root design brief, view narrative, domain design intent, constraints and trade-offs; Doctor blocks tabulated architecture coverage.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.9`.
- Current `ssot-preflight` `metadata.protocol_version` is `2.9` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Root design brief | `architecture/README.md` | Check whether the root explains in natural language the mission, audience, optimization priorities, current priorities, non-goals, success criteria and what future agents must preserve. | Root is not pure table or pure index; can state its design stance in 1 minute. |
| Design-intent views | `architecture/views/operating-model.md`, `critical-journeys.md`, `current-target-gap.md` | Run absorption checks on goals, priorities, non-goals, acceptance criteria, main paths and partially-landed intents from PRDs, design docs, historical architecture notes. | When such source material exists, design intent enters views; without material, record gap/unknown without fabrication. |
| Domain design-intent block | `architecture/domains/` or legacy direct child domains | Check whether each domain states design intent, design constraints, trade-offs / rejected simplifications and what future agents must preserve. | Domain can explain why the boundary exists, what cannot be broken, which shortcuts were rejected. |
| Tabulated architecture guard | architecture root/views/domains | Check whether architecture is mostly tables, lacking design narrative. | Tables only as index / evidence ledger; design judgement must have natural-language narrative. |
| Source-material absorption matrix | `STATUS.md` | Check whether source-material absorption covers the PRD/docs/ADR/runbook/user material used in this design-layer audit. | Materials have classification, authoritative location, absorption state, conflict/adjudication. |
| Doctor check | Doctor output, `STATUS.md` | Add or equivalently run `[DESIGN-INTENT]` check. | stale items fixed, adjudicated or recorded no-op; only after pass may `tracked_skill_version` advance. |

**Doctor tags**:

- `[DESIGN-INTENT]`: root/view/domain lacks design intent, priorities, non-goals, success criteria, constraints or trade-off narrative, or consists mainly of tables.

**Not required**:

- Not required to mechanically migrate existing direct child-domain projects to `views/ + domains/`.
- Not required to add top-level `SSOT/design/`.
- Not required to fabricate design goals for projects without source material or evidence; record gap/unknown.
- Not required to rewrite all existing SSOT files to the latest template; only update affected authoritative locations.

### v2.8

**Upgrade goal**: upgrade architecture from giant root README / homogeneous direct child-domain tables to a Views + Domains protocol; clearly define the design-absorption route for source material and long-term record absorption state in `STATUS.md`.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.8`.
- Current `ssot-preflight` `metadata.protocol_version` is `2.8` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Architecture-structure compatibility | `architecture/README.md`, `architecture/views/`, `architecture/domains/`, legacy direct child domains | Determine whether the project is single-level, legacy direct child-domain, or views/domains. | Old structures stay compatible; no mechanical migration. New bootstraps or user-requested reorganizations use `views/ + domains/`. |
| Root architecture entry | `architecture/README.md` | Check whether the root can quickly explain system principles / operating model, primary journeys, core invariants, view index, Domain index and Current / Target / Gap summary. | Root is not a pure link page or a full audit report; long flow/state/failure detail sinks to views/domains. |
| Views authoritative location | `architecture/views/` | Run absorption checks on system goals, operating philosophy, user/runtime main paths, critical journeys, current-target-gap from PRD, design docs, historical architecture notes. | When such source material exists, goals/philosophy/main paths enter operating-model, end-to-end flows enter critical-journeys, global gaps enter current-target-gap; without material, record gap/unknown. |
| Domain authoritative location | `architecture/domains/` or legacy direct child domains | Check whether state/resource ownership, locks, contracts, failure recovery, verification evidence are in the domain README, not piled at root or docs. | Each domain has `why separate` + independence signal; state/contract/failure/verification details have evidence and diagrams. |
| Source-material absorption matrix | `STATUS.md` | Check whether a source-material absorption table exists and covers README/docs/ADR/runbook/PRD/user material that was read or changed. | Each material has classification, authoritative location, absorption state, conflict/adjudication; `stale/conflict` cannot just be marked and skipped. |
| Forbid top-level design split | `SSOT/` | Check whether `SSOT/design/` or other parallel design-authority surface is added or relied on. | No new top-level design; design facts go into architecture views/domains, decisions, bugs, gotchas, testing and other authoritative locations. |
| Doctor check | Doctor output, `STATUS.md` | Add or equivalently run `[ARCH-ROOT]`, `[ARCH-VIEWS]`, `[ARCH-DOMAINS]`, `[SOURCE]` checks. | stale items fixed, adjudicated or recorded no-op; only after pass may `tracked_skill_version` advance. |

**Doctor tags**:

- `[ARCH-ROOT]`: architecture root cannot build a quick mental model, or piles all detail at root.
- `[ARCH-VIEWS]`: system goals, operating philosophy, critical journeys or current-target-gap still sit in source material, not absorbed into views.
- `[ARCH-DOMAINS]`: state/resource/contract/failure/verification detail not in the domain authoritative location, or domain lacks validity evidence.
- `[SOURCE]`: long-lived facts in source material not absorbed, or `stale/conflict` only marked, not processed.

**Not required**:

- Not required to mechanically migrate existing direct child-domain projects to `views/ + domains/`.
- Not required to add top-level `SSOT/design/`.
- Not required to fabricate view content for projects without relevant source material; record gap/unknown.

### v2.7

**Upgrade goal**: bring the project documentation language lock into SSOT protocol state, ensuring all SSOT Markdown uses the project's existing documentation language or the language the user explicitly chose.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.7`.
- Current `ssot-preflight` `metadata.protocol_version` is `2.7` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Documentation-language field | `SSOT/STATUS.md` | Check whether `documentation_language` and `documentation_language_evidence` exist. | The fields exist and record the language lock and evidence; when evidence is mixed, insufficient or there are no detectable documents, the user has been asked and the choice recorded. |
| Language-detection evidence | `SSOT/.bootstrap/recon.md` (if still in bootstrap), `STATUS.md` | Detect only from root README, `docs/`, ADR, runbook, subsystem README, user-provided material; ignore code blocks, commands, paths, API names, enum values, code identifiers and verbatim quotations. | `documentation_language_evidence` can explain the source and judgement; do not fall back to current conversation language. |
| Existing SSOT language consistency | All SSOT Markdown touched by this upgrade | Check whether the SSOT body, headings and table labels being modified obey the locked language. | New/modified content in this run uses the locked language; code identifiers, paths, commands, API names, enum values and verbatim quotations stay in original. |
| Language-switch governance | `STATUS.md` open adjudications / stop-review gate | If source-material language has changed or SSOT has mixed languages, judge whether a language-switch adjudication is needed. | Language switch does not happen automatically; when needed, there is an adjudication and a `doc-language-change` / `documentation_language` review. |
| Doctor check | Doctor output, `STATUS.md` | Add and run, or equivalently run, the `[DOC-LANGUAGE]` check. | `[DOC-LANGUAGE]` stale items fixed, adjudicated or recorded no-op; only after pass may `tracked_skill_version` advance. |
| Template-instantiation discipline | New/modified SSOT files in the project | Check whether the agent translates the template structure per the language lock instead of copying the template's original language. | Do not mechanically rewrite all existing SSOT; but from this upgrade onward, new changes must obey the language lock. |

**Doctor tags**:

- `[DOC-LANGUAGE]`: `documentation_language` missing, evidence insufficient, SSOT text deviates from the locked language, or language switch not in adjudication/review.

**Not required**:

- Not required to mechanically rewrite all existing SSOT Markdown.
- Not required to guess the language when language evidence is insufficient; must ask the user.
- Not required to auto-switch SSOT language when subsequent source-material language changes.

### v2.6

**Upgrade goal**: explicitly bring architecture evolution, bug-record granularity, high-frequency task entries and Doctor verification requirements into the SSOT maintenance protocol, and add a skill-protocol awareness waterline.

**Baseline scope**:

- Legacy projects with missing `tracked_skill_version`.
- Projects with `tracked_skill_version` less than `2.6`.
- Current `ssot-preflight` `metadata.protocol_version` is `2.6` and project SSOT has not been audited per this entry.

**Impact checklist**:

| Check | Affected area | Audit action | Done criterion |
|---|---|---|---|
| Bug failure-mode granularity | `bugs/`, `gotchas/`, `testing/`, related `architecture/` domain | Find whether `critical` / `major` / `recurred` bugs are recorded only under a broad theme; same symptoms with multiple root causes must be split, same root-cause recurrence must append a recurrence timeline. | High-impact bug entries contain trigger condition, symptom, root cause, fix pattern, preventive test, related gotcha / architecture / decision; without such bugs, record no-op. |
| Evolution / migration ledger | `architecture/README.md` and related domain README | Check whether git history, ADR, docs/source material or current code show major architectural migration, legacy-surface removal, compatible paths, deprecated concepts or "do not revive" old approaches. | When evidence exists, record old form, current replacement form, compatibility status, do-not-revive concepts and evidence in the corresponding architecture `evolution / migration ledger`; without evidence, write `not_applicable` and reason. |
| Task-entry map thin index | `SSOT/README.md` | Check whether git history, commit audit or long-running sessions show high-frequency / high-risk development task clusters. | Maintain a task-entry map only when task clusters exist, and link only to authoritative locations; without clusters, write `not_applicable` and do not write playbook body or a second fact source. |
| Doctor check | `STATUS.md`, Doctor output, related authoritative locations | Run or equivalently run the Doctor `BUG-GRANULARITY`, evolution / migration ledger, thin-documentation/source-material, architecture diagram/coverage, `SKILL-PROTOCOL` checks. | Doctor stale items fixed or recorded no-op; only after `SKILL-PROTOCOL` passes may `tracked_skill_version` advance. |
| Skill protocol waterline | `SSOT/STATUS.md` | Add the `tracked_skill_version` field to record the protocol version applied to the project SSOT. | After an independent reviewer returns `no-more-required-changes` on the protocol upgrade review, update `tracked_skill_version` to `2.6`. |

**Doctor tags**:

- `[BUG-GRANULARITY]`: high-impact bug records too coarse.
- `[EVOLUTION]`: architecture migration or deprecated concept not in evolution / migration ledger.
- `[THIN-DOCS]` / `[SOURCE]`: long-lived facts stay in README/docs/ADR/source material.
- `[SKILL-PROTOCOL]`: `tracked_skill_version` missing or behind current `ssot-preflight` `metadata.protocol_version`.

**Not required**:

- Not required to rewrite all existing SSOT files to the latest template.
- Not required to fabricate evolution / migration ledger entries for projects with no relevant history evidence.
- Not required to maintain task-entry map body for projects without high-frequency / high-risk task clusters.
