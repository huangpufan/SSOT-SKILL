# 开发工作流

<!-- 行文对象：implementation-delegator。先写改动情境与决定，再写路径、结果/
     恢复，最后给命令与证据。 -->

<!-- 完整性权威：reader-quality.md C01-C09、PR01-PR16 与适用 Q01-Q21。
     covered 流程使用精确的策略说明与有限资产清单；不适用项写原因和证据指针。 -->

> 本区域记录如何把项目跑起来，以及新代理写代码时必须遵守的开发约定。
> 只记录长期有用的含义、前置条件和风险；完整脚本源码以代码仓库为准。

## 何时由谁使用

<用白话说明哪类贡献者使用本流程、什么改动或症状会触发它、需要怎样的仓库与
运行环境，以及需要哪个角色或权限。给出一个第一天即可理解的具体场景。>

## 为什么采用这套开发方法

<先解释策略再列步骤：哪些约定和不变量让改动保持一致，为什么采用这个顺序，
哪个约束或历史失败排除了最接近的替代，接受了什么取舍，以及什么证据足以
改变方法。>

## 标准路径与分支

<按顺序讲完整开发故事：准备输入、启动正确服务、完成改动、处理重要分支或
例外，并说明任何改变状态或不可逆的副作用。重复、并发或部分完成会改变安全时，
说明其行为（PR14）；适用 Q01-Q21 检查从 STATUS 路由。>

## 产物与验收

<写明可观察的产物或结果，以及什么检查才算验收。命令存在不等于验收；要说明
成功结果长什么样。>

## 失败、恢复与交接

<说明如何发现失败、何时停止、哪些步骤可以重试或回滚、哪些无法逆转，以及
需要升级给哪个所有者。>

## 怎样复现，以及何时重新检查

<给出规范的可复现命令路径、证据来源、当前与预期状态，以及会使本流程失效的
文件或事件。>

## 开发一眼看懂

| 读者问题 | 先读哪里 | 权威所有者 | 证据方向 | 停止条件或风险 |
|---|---|---|---|---|
| 如何启动本地开发？ | [本地运行](#本地运行) | 本文件 | `package.json` / `Makefile` / `Dockerfile` / 文档 | |
| 哪些脚本/工具常用且有前置条件？ | [脚本/工具目录](#脚本--工具目录) | 脚本目录 | 脚本目录 / CI / `Makefile` | |
| 新增功能时遵守哪些代码模式？ | [模式语言](#模式语言) | 本文件 | 代码评审 / 静态检查配置 / 示例 | |

## 本地运行

用短叙述说明本仓库的开发路径：依赖如何安装，服务如何启动，哪些步骤必须先后执行。

| 场景 | 命令 | 目的 | 必需准备 | 证据 | 已知风险 |
|---|---|---|---|---|---|
| | | | | `package.json` / `Makefile` / `Dockerfile` / 文档 | |

## 脚本 / 工具目录

> 这是开发流程会创建、读取、修改、验证、交接或退役的稳定脚本与工具有限清单。
> 不复制源码；每个真实资产只写一次。确实一个都没有时，删除示例行并写：
> `无稳定资产：原因=<具体理由>；负责人=[责任所有者](<可解析路径>)；复核条件=<可观察事件>。`

| 资产 | 类别 | 用途 | 选择规则 | 所有者 | 证据 | 风险 | 退役或替换触发条件 |
|---|---|---|---|---|---|---|---|
| | script / tool / 构建 / 开发服务 / 代码生成 / 检查与格式化 / 诊断 / 其它 | | | | | | |

## 模式语言

> 只记录静态检查或格式化工具不能自动保证、且新代理容易违反的编码约定。

| 模式 | 什么时候使用 | 为什么重要 | 证据 | 风险 |
|---|---|---|---|---|
| | | | | |

## 端到端骨架流程

| 功能类型 | 需要触碰的权威位置 | 代表性示例 | 验证方式 | 风险 |
|---|---|---|---|---|
| | | | | |

## 开放缺口

| 缺口或未知项 | 所需证据 | 阻断级别 |
|---|---|---|
| | | `blocking`（阻断）/ `non-blocking`（不阻断） |
