# <术语>

<!-- 行文对象：implementation-delegator。每节先用平实散文，再放表格；表格是索引而非段落。
     遵循 reader-quality.md。 -->

<!-- 完整性权威：reader-quality.md C01-C09、G01-G08 与适用 Q01-Q21 词汇。
     包括正面定义、决策后果、例子/反例、
     唯一所有者、相关范围与失效方向。术语表通过 Area Status 与语义停止审查管理；
     每文件 intent_recovery 只适用于产品与架构。 -->

**一句话定义**：<不超过 25 字；正向定义；禁止以"X 不是 Y"开头>。

## 扩展定义

<一段；当一句话不足以涵盖术语的完整范围、生命周期或约束时必填>

## 为什么重要

<说明正确理解本术语会改变哪个读者决策、产品承诺、运行时边界、流程闸门或
记录解释。>

## 例子与反例

- **例子** —— <一个术语确实适用的仓库具体场景>。
- **反例** —— <最接近但不成立的场景，以及原因>。

## 在哪里使用

- `<area>/<file>` § <小节> — <一句话：本术语在此处为什么重要>
- `<area>/<file>` § <小节> — <一句话>

## 容易混淆

- **<兄弟术语>** — <一句话区分；用正向定义说明边界>
- **<兄弟术语>** — <一句话区分>

<!-- G07：记录规范拼写；若不同，再记录别名或缩写、用户可见标签、机器 token
     与翻译约束。先解释人应使用哪种形式，再列机器形式。 -->

## 证据定位

- `[CORE-REF: <area>/<file>.md#anchor]` — <本术语所指的产品、架构、流程、记录、
  数据结构或契约事实由哪里维护；这里不是第二份术语定义>
- `path:src/myapp/...:LNN` 或 `tests/...::test_*` — <代码/测试定位，若术语已在代码落地>

## 范围与失效

<写出相关术语；明确本 glossary 条目是唯一术语定义；再说明什么代码、数据结构
或决策变化会触发本定义，以及依赖这个术语的文档或功能复核。>

## 术语生命周期

<状态写 `active`（使用中）、`deprecated`（已弃用）或 `retired`（已退休）。
已弃用或已退休时，写明替代词、迁移方向和完成退休的触发事件。>

<!--
模板使用须知（v2.51）：

1. 这是独立文件形态。既有 README/主题聚合可以每个术语使用唯一二级标题锚点；
   术语家族清单链接该精确锚点。
2. 正向定义由 Doctor 14I（cell-is-not-a-paragraph）与 15F（`[VOCAB-PROSE-FORK]`）守。
   反向定义只能写在 `## 容易混淆`，不能写在标题。
3. `## 用于` 是反向索引：读者从这里跳到正在消费本术语的非 glossary owner。
4. KISS mini-card 允许形式（v2.51，详见 `SKILL_STYLE.md`）：在消费者 owner
   的散文中，本术语可以以
   `**术语** (def: <≤ 15 字的子句> → [CORE-REF: glossary/<term>.md])`
   形式首次出现，不违反 15F。
-->
