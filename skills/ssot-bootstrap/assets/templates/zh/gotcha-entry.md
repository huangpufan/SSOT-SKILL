---
id: GOT-NNNN
record_status: current
hazard_state: active
status: active
created_on: YYYY-MM-DD
updated_on: YYYY-MM-DD
owner: <role-or-owner-path>
trigger: <task-pattern-or-path-glob>
---

# <陷阱标题>

<!-- 单条文件形态：开头的 `id` 是条目编号。既有主题文件为每条使用稳定 H2
     `## GOT-NNNN`，下一行再用 H3 或粗体写易读标题；集合索引链接
     `#got-nnnn`。下一个 H2 前必须写一行 **记录状态 / 危险状态**：
     `current` / `active`；主题聚合是兼容形态。 -->

<!-- 行文风格：implementation-delegator。第一屏先给触发条件、决定、安全动作和
     当前状态；仓库术语首次出现时解释。 -->

<!-- 完整性权威：reader-quality.md C01-C09、R01-R16 与适用 Q01-Q21。 -->

## 快速入口

- **何时出现** —— <具体操作、路径或症状>。
- **决定** —— 不要 <危险的直觉动作>；改为 <安全动作>。
- **先查证据** —— <稳定路径、符号、测试、日志或决策>。
- **最小预防检查** —— `<command-or-test>`。
- **当前状态** —— `active` / `resolved`；<一句话指向拥有理由的正文段落>。

`record_status` 表示本条目仍是当前路由知识、已归档或已被取代；
`hazard_state` 另行表示陷阱仍存在还是已经解除。兼容字段 `status` 只镜像
`hazard_state`。

## 背景与已观察事实

<!-- 写明发现时间、直接观察、source snapshot 或环境，以及哪些仍是推测或判断。 -->

描述一个合理动作怎样在具体场景中产生错误结果。把直接观察的事实和基于事实作出
的解释分开。

## 为什么直觉动作危险

<!-- 解释原因、影响、严重性、受影响范围，以及不可逆或静默副作用。按 R15/R16
     写明受影响版本、平台、环境、租户/工作区、数据类别、兼容窗口，以及
     安全/隐私/合规/客户暴露或通知义务。 -->

从诱人动作讲到可见或潜在失败的因果链。指出谁或什么受影响，以及普通检查为
什么可能漏掉它。

## 更安全的替代

**不要**：<不安全动作>。

**改为**：<安全路径，以及它为什么保留所需边界>。

<!-- 有多个安全回应时，记录考虑过的替代与取舍。 -->

## 复现与预防

安全可运行时，描述最小可靠复现；随后说明防止再次发生的代码保护、流程步骤、
测试或评审规则。复现具有破坏性或成本时，写明替代证据和所需权限。

## 验证，以及证据何时失效

列出证明陷阱、替代方案和当前状态的证据。写明观察时间、证据层级与局限，以及哪个
路径、API、配置、架构或测试变化会触发复核。

| 结论 | 证据 | 观察时间或源码状态 | 限制 |
|---|---|---|---|
| | | | |

## 所有者与跟进

写出条目所有者、能移除陷阱的实现或流程所有者、下一项具体行动，以及必须跟进的
触发条件。

## 解决或失效

`active` 写出使陷阱不再适用的可证伪条件。`resolved` 写出移除它的改动或
决策，以及确认旧触发条件已消失的检查。保留历史教训，不要把条目改写得像
陷阱从未存在。
